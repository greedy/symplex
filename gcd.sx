function gcd(a,b) {
  if (b == 0) { a; }
  else { gcd(b, a % b); };
}

suite {
  a = symbolic();
  b = symbolic();
  assume(a < 5);
  assume(b < 5);
  assume(a > 0);
  assume(b > 0);
  r = gcd(a,b);
  test {
    check(a % r == 0);
    check(b % r == 0);
  }
  test {
    c = symbolic();
    assume(a % c == 0);
    assume(b % c == 0);
    check(c <= r);
  }
}
